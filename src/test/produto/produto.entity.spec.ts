import {
    describe, expect, it, jest,
  } from '@jest/globals';
import { ProdutoEntity } from "../../produto/produto.entity.js"

describe('Teste modelo produto', () => {
  it('Produto deve ser criado com sucesso', () => {
    let produto;

    beforeEach(() => {
      produto = new ProdutoEntity();

      produto.
    });

  }
  )
})